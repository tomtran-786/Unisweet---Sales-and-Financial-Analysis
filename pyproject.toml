[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unisweet-sales-master"
version = "1.0.0"
description = "Flat Sales master generator for UniSweet"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "openpyxl>=3.1,<4",
  "pandas>=2.2,<4",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<10",
]

[project.scripts]
unisweet-sales-master = "unisweet_analysis.sales_master_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/unisweet_analysis"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
